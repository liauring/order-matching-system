require('dotenv').config({ path: __dirname + '/./../.env' })
const socket = require('../util/socket')
let { db } = require('../util/mongodb')
let { mysqldb } = require('../../broker/util/mysql')

;(async () => {})()
